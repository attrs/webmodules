# webmodules examples - bootstrap

## Installation
```sh
$ npm install -g webmodules
$ git clone https://github.com/attrs/webmodules.git
$ cd webmodules/examples/bootstrap
$ wpm install
$ wpm up 9000 -o
```
