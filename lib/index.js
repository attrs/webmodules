var npminstall = require('./npm');
var bowerinstall = require('./bower');

module.exports = {
  install: function(options, done) {
    
  },
  uninstall: function(options, done) {
    
  },
  publish: function(options, done) {
    
  }
};